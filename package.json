{
  "dependencies": {
    "concurrently": "^9.1.0"
  },
  "scripts": {
    "server": "cd server && npm run dev",
    "frontend":"cd client && npm run dev",
    "dev":"concurrently -n \"frontend,server\" --prefix-colors \"bgGreen,bgRed\" \"npm run frontend\" \"npm run server\""
  }
}
