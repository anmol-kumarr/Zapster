import { RootState } from "@reduxjs/toolkit/query";
import { useSelector } from "react-redux";
import { io } from "socket.io-client";





interface ConnectProps {
    userId: string
}



const connectSocket = ({ userId }: ConnectProps) => {




}

export default connectSocket